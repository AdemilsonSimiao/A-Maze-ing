Mandatory part
IV.1 Summary
You will implement a maze generator in Python that takes a configuration file, generates
a maze, possibly perfect (with a single path between entrance and exit), and writes it to a
file using a hexadecimal wall representation. You will also provide a visual representation
of the maze and organize your code so that the generation logic can be reused later.
IV.2 Usage
Your program must be run with the following command:
python3 a_maze_ing.py config.txt
• a_maze_ing.py is your main program file. You must use this name.
• config.txt is the only argument. It is a plain text file that defines the maze
generation options. You can use a different filename.
Your program must handle all errors gracefully: invalid configuration, file not found, bad
syntax, impossible maze parameters, etc. It must never crash unexpectedly, and must
always provide a clear error message to the user.
IV.3 Configuration file format
The configuration file must contain one ‘KEY=VALUE‘ pair per line.
Lines starting with # are comments and must be ignored.
The following keys are mandatory:
Key Description Example
WIDTH Maze width (number of cells) WIDTH=20
HEIGHT Maze height HEIGHT=15
ENTRY Entry coordinates (x,y) ENTRY=0,0
EXIT Exit coordinates (x,y) EXIT=19,14
OUTPUT_FILE Output filename OUTPUT_FILE=maze.txt
PERFECT Is the maze perfect? PERFECT=True
7
A-Maze-ing This is the way
You may add additional keys (e.g., seed, algorithm, display mode) if useful.
A default configuration file must be available in your Git repository.
IV.4 Maze Requirements
• The maze must be randomly generated, but reproducibility via a seed is required.
• Each cell of the maze has between 0 and 4 walls, at each cardinal point (North,
East, South, West).
• The maze must be valid, meaning:
◦ Entry and exit exist and are different, inside the maze bounds.
◦ The structure ensures full connectivity and no isolated cells (except the ’42’
pattern, see below).
◦ As entry and exit are specific cells, there must be walls at the external borders.
◦ Your generated data must be coherent: each neighbouring cell must have the
same wall if any. E.g., it is forbidden to have a first cell with a wall on the
east side, and the second cell behind that wall without a wall on the west side.
• The maze can’t have large open areas. Corridors can’t be wider than 2 cells.
For example, you can have 2x3 or 3x2 open area, but never a 3x3 open area.
• When visually represented (see below), the maze must contain a visible “42” drawn
by several fully closed cells.
• If the PERFECT flag is activated, the maze must contain exactly one path between
the entry and the exit (i.e., it must be a perfect maze: no loops at all).
• If the PERFECT flag is not activated (the default), the maze must instead be a board
directly usable by a Pac-Man-like game. Concretely:
◦ every corridor is reachable (full connectivity), so the whole board can be filled
with pac-gums and remains winnable;
◦ the four corners and the centre are open corridors (the ghosts and super-pacgums sit in the corners, the player starts in the centre);
◦ it offers at least two independent routes (loops), so that a chased player
always has an alternative (a perfect maze, or a perfect maze with merely one
wall removed (a single loop), is therefore not acceptable in this mode);
◦ dead-ends should stay rare (a couple are tolerated); a board with no dead-end
at all is the ideal and is rewarded as a bonus (see the Bonuses chapter).
The “42” pattern may be omitted in case the maze size does not allow
it (i.e. too small). Print an error message on the console in that
case

A-Maze-ing This is the way
The two generation modes on the same grid: PERFECT=True forces a single winding path (every other
corridor is a dead-end), while the default PERFECT=False keeps at least two independent routes open so
a chased player always has an alternative. The shortest path is highlighted in both.
IV.5 Output File Format
The maze must be written in the output file using one hexadecimal digit per cell, where
each digit encodes which walls are closed:
Bit Direction
0 (LSB) North
1 East
2 South
3 West
• A wall being closed sets the bit to 1, open means 0.
Example: 3 (binary 0011) means walls are open to the south and west. Or A
(binary 1010) means that east and west walls are closed.
• Cells are stored row by row, one row per line.
• After an empty line, the following 3 elements are inserted in the output file on 3
lines:
◦ the entry coordinates, the exit coordinates, and the shortest valid path from
entry to exit, using the four letters N , E , S , W .
• All lines end with a \n .
9
A-Maze-ing This is the way
In conjunction with its specific configuration file, this output file could be tested automatically by a Moulinette. An analysis script (maze_analyzer.py) is also provided with
this subject. Given an output file, it checks that the wall encoding is coherent (each
shared wall is encoded the same way by both neighbouring cells) and reports whether
the maze is perfect (a single path, as required with PERFECT=True) or a playable board
(no dead-ends besides the “42” pattern and several independent routes, as required by
default). You may use it to check that your generator produces the mode requested by
the configuration file.
Output file example

Visual representation
Your program must provide a way to display the maze visually, using either:
• Terminal ASCII rendering, or
• A graphical display using the MiniLibX (MLX) library.
The visual should clearly show walls, entry, exit, and the solution path.
User interactions must be available while the program is running, and must include, at
least, the following tasks:
• Re-generate a new maze and display it.
• Show/Hide a valid shortest path from the entrance to the exit.
• Change maze wall colours.
• Optional: set specific colours to display the “42” pattern.
You can add extra user interactions.
11
A-Maze-ing This is the way
Terminal default rendering of the maze
Different maze, shortest path and wall colours
12
A-Maze-ing This is the way
Maze rendering using Mlx

Code reusability requirements
You must implement the maze generation as a unique class (e.g., ‘MazeGenerator‘) inside
a standalone module that can be imported in a future project.
You must provide a short documentation describing how to:
• Instantiate and use your generator, with at least a basic example.
• Pass custom parameters (e.g., size, seed).
• Access the generated structure, and access at least a solution.
The maze generator module grants access to the maze structure, but it
REGRAS DO PROJETO A-MAZE-ING
============================

Este arquivo reorganiza o enunciado em requisitos e etapas de execução. O texto
original exige um gerador reutilizável, um arquivo de saída hexadecimal, uma
representação visual e tratamento claro de erros.


1. ESTADO ATUAL DO PROJETO
--------------------------

Situação verificada em 2026-09-20 rodando os testes, o flake8, o mypy e o
programa com o config.txt do projeto.

Legenda:
[x] feito e verificado (por teste ou por execução);
[~] existe, mas está parcial ou tem problema conhecido (descrito na linha);
[ ] pendente.

1.1 Arquivos

[x] config.txt existe na raiz e contém WIDTH, HEIGHT, ENTRY, EXIT,
	OUTPUT_FILE, PERFECT e SEED.
[x] cli/errors.py define ConfigError.
[~] cli/config_parser.py lê e valida a configuração. Problemas:
	- linha com chave vazia, valor vazio ou chave duplicada levanta ValueError,
	  e não ConfigError;
	- SEED é exigida como chave obrigatória (o enunciado a trata como extra);
	- WIDTH e HEIGHT não são validados como maiores que zero (a mensagem diz isso,
	  mas só o erro de "não é inteiro" é tratado);
	- erro de digitação na mensagem: "interger".
[~] mazegen/generator.py contém Cell, MazeGenerator e a geração por DFS.
	- DFS sem o 42: [x] gera árvore perfeita (ex.: 10x6 tem 59 passagens para 60
	  células, tudo alcançável, paredes coerentes);
	- padrão 42 e modo não perfeito: [~] ver 1.2.
[x] mazegen/solver.py contém MazeSolver com busca BFS (3 testes passam).
[x] mazegen/encoder.py implementa a serialização hexadecimal (4 testes passam).
[~] display/ascii_renderer.py desenha paredes, entrada (E), saída (X) e caminho
	(.). Problemas:
	- o renderer já usa cores ANSI, mas 3 dos 4 testes do renderer ainda esperam
	  texto sem cor e falham;
	- o parâmetro wall_color existe, mas nada o usa;
	- pattern_cells existe, mas a_maze_ing.py nunca o passa (o 42 não é destacado).
[~] a_maze_ing.py gera, resolve, imprime o ASCII uma vez e grava o OUTPUT_FILE.
	Problemas:
	- só trata FileNotFoundError: com um config inválido o programa mostra um
	  traceback (ConfigError não é capturado), o que viola "nunca quebrar";
	- não há nenhuma interação (regenerar, mostrar/ocultar caminho, cores);
	- não trata erros de gravação do arquivo de saída.
[ ] display/mlx_renderer.py está vazio (só será necessário se o MLX for
	escolhido; hoje a escolha é a renderização ASCII).
[ ] LICENSE.md está vazio.
[ ] pyproject.toml está vazio.
[ ] mazegen-1.0.0-py3-none-any.whl tem 0 bytes (não é um pacote válido).
[ ] docs/algorithm_notes.md está vazio.
[~] README.md só contém anotações de Git; falta todo o conteúdo exigido
	(seção 8).
[~] Makefile só tem uma linha de texto com os nomes (install, run, debug, clean,
	lint, lint-strict); ainda não há alvos reais.
[~] tests/: 21 testes, 18 passam e 3 falham (renderer, ver acima). Não existem
	testes para o padrão 42, para as regras do modo PERFECT=False, para
	conectividade total, para a regra da área aberta 3x3 nem para o a_maze_ing.py.
[x] mypy: sem erros nos 16 arquivos.
[ ] flake8: 139 avisos (sobretudo W191 tabulação, W293 espaços em linhas em
	branco, E302/E305 linhas em branco, W292 sem quebra de linha no fim, F401 e
	F841 imports/variáveis sem uso).
[ ] maze_analyzer.py (analisador fornecido com o enunciado) não está no
	projeto, então a saída ainda não foi verificada com ele.

1.2 Problemas de comportamento verificados

Padrão 42 (mazegen/generator.py, _apply_42_pattern):
- ele é aplicado depois de o labirinto pronto e fecha as 4 paredes das
  células do 42, mas não fecha as paredes dos vizinhos que davam para elas.
  Com 20x15 e seed 42 aparecem 32 pares de paredes incoerentes, e só 174 das
  300 células são alcançáveis a partir da entrada. Com as seeds 0 a 4 o solver
  não encontra caminho entre entrada e saída (retorna vazio) e o programa não
  avisa;
- generate() chama o padrão quando WIDTH >= 8 e HEIGHT >= 5, mas o próprio
  padrão exige pelo menos 14x8. Entre esses tamanhos só imprime "Pattern 42
  skipped: maze too small.", o que cumpre o aviso pedido, mas a condição está
  duplicada e inconsistente;
- o 42 é desenhado com 27 células totalmente fechadas (0xF), como o enunciado
  pede;
- tamanho do 42: o código desenha um padrão de 11x7 (dois dígitos de 5x7) e
  exige pelo menos 14x8. O exemplo do subject (figura do capítulo IV.4) mostra
  dígitos menores, de 3x5, formando um padrão de 7x5 no centro do labirinto.
  Decidir o tamanho do 42 e o tamanho mínimo do labirinto a partir dele.

Modo PERFECT=False (mazegen/generator.py, _add_loops):
- só abre paredes aleatórias (cerca de WIDTH*HEIGHT/12 tentativas);
- não garante dois caminhos independentes, poucos becos sem saída nem o limite
  de área aberta 3x3 (nas amostras testadas não apareceu 3x3, mas não há
  garantia nem teste);
- quebra em tamanhos pequenos: com 2x2 levanta ValueError (randint(1, w - 2)).

Modo PERFECT=True sem o 42 funciona e respeita bordas e coerência.


2. EXECUÇÃO OBRIGATÓRIA
-----------------------

O programa deve ser executado com:

	python3 a_maze_ing.py config.txt

Requisitos:

- a_maze_ing.py deve ser o arquivo principal;
- config.txt é o único argumento;
- erros de arquivo, sintaxe, configuração e parâmetros impossíveis devem ser
  tratados com mensagens claras;
- o programa não deve quebrar inesperadamente.

NOTA (estado atual): hoje só FileNotFoundError vira mensagem clara. Config
inválido (ConfigError e ValueError do parser) e falhas ao gravar o arquivo de
saída ainda quebram com traceback.


3. FORMATO DA CONFIGURAÇÃO
--------------------------

Cada linha válida tem o formato KEY=VALUE. Linhas vazias e linhas iniciadas
com # devem ser ignoradas.

Chaves obrigatórias:

	WIDTH=20
	HEIGHT=15
	ENTRY=0,0
	EXIT=19,14
	OUTPUT_FILE=maze.txt
	PERFECT=True

Chaves adicionais, como SEED, são permitidas. Deve existir uma configuração
default versionada no repositório.

NOTA (estado atual): o parser exige SEED, embora o enunciado só a permita como
chave extra. Decidir se SEED passa a ser opcional (sem SEED = aleatório).


4. REQUISITOS DO LABIRINTO
--------------------------

O labirinto deve:

- ser aleatório e reproduzível quando uma seed for usada;
- ter células com paredes Norte, Leste, Sul e Oeste;
- manter ENTRY e EXIT dentro dos limites e em posições diferentes;
- ser totalmente conectado, sem células isoladas;
- manter as bordas externas fechadas;
- manter paredes compartilhadas coerentes entre células vizinhas;
- não criar áreas abertas maiores que 2 células de largura;
- conter um padrão visual 42 quando o tamanho permitir;
- informar no console quando o padrão 42 não puder ser criado.

Modo PERFECT=True:

- deve existir exatamente um caminho entre entrada e saída;
- não pode haver loops.

Modo PERFECT=False:

- todos os corredores devem ser alcançáveis;
- os quatro cantos e o centro devem ser corredores abertos;
- devem existir pelo menos dois caminhos independentes;
- becos sem saída devem ser raros.


5. FORMATO DO ARQUIVO DE SAÍDA
------------------------------

Cada célula ocupa um dígito hexadecimal. Os bits representam paredes fechadas:

	bit 0 (1): Norte
	bit 1 (2): Leste
	bit 2 (4): Sul
	bit 3 (8): Oeste

Parede fechada = bit 1. Passagem aberta = bit 0.

As células são escritas linha por linha. Depois das linhas do labirinto, o
arquivo deve conter:

1. uma linha vazia;
2. as coordenadas da entrada;
3. as coordenadas da saída;
4. o caminho mínimo usando N, E, S e W.

Todas as linhas devem terminar com \n.


6. VISUALIZAÇÃO E INTERAÇÕES
----------------------------

Deve existir uma visualização ASCII ou MiniLibX que mostre:

- paredes;
- entrada;
- saída;
- caminho da solução.

Durante a execução, deve ser possível:

- gerar e exibir outro labirinto;
- mostrar ou ocultar o caminho mínimo;
- alterar a cor das paredes;
- opcionalmente destacar o padrão 42.

NOTA (estado atual): existe só a renderização ASCII, exibida uma vez. Ainda não
existe nenhuma das interações obrigatórias (regenerar, mostrar/ocultar caminho,
mudar a cor das paredes) nem o destaque opcional do 42.

REFERÊNCIA (exemplos de saída do subject):
- Modos de geração (figura do capítulo IV.4): PERFECT=True mostra um único
  caminho sinuoso; PERFECT=False (o padrão) mostra um tabuleiro com loops, sem
  becos sem saída. Nos dois o caminho mais curto aparece destacado e o 42 fica
  no centro, em células fechadas.
- Cores da figura: entrada rosa, saída vermelha, 42 cinza claro, caminho
  mais curto ciano. A saída não precisa ficar no canto oposto à entrada.
- Renderização no terminal (figura do capítulo de visualização): paredes
  desenhadas como blocos preenchidos, com entrada, saída e 42 coloridos, e
  abaixo do labirinto este menu:

      === A-Maze-ing ===
      1. Re-generate a new maze
      2. Show / Hide the shortest path
      3. Rotate the wall colours
      4. Quit
      Choice? (1-4):

  Ou seja, as interações obrigatórias são um menu numerado que repete até o
  usuário escolher 4 (Quit).


7. REUTILIZAÇÃO E EMPACOTAMENTO
-------------------------------

O gerador deve ser uma classe reutilizável em um módulo independente. A
documentação deve explicar:

- como instanciar e usar o gerador;
- como passar tamanho, seed e outros parâmetros;
- como acessar a estrutura gerada;
- como acessar uma solução.

O pacote deve:

- ter nome no formato mazegen-*;
- estar disponível na raiz do repositório como .whl ou .tar.gz;
- conter tudo o que é necessário para ser reconstruído com ferramentas padrão;
- incluir LICENSE.md permitindo reutilização e distribuição.

NOTA (estado atual): LICENSE.md, pyproject.toml e o .whl existem, mas estão
vazios. A documentação de uso do gerador ainda não foi escrita.


8. DOCUMENTAÇÃO DO README.md
----------------------------

A primeira linha deve ser italicizada e informar que o projeto foi criado como
parte do currículo 42 pelos logins da equipe.

O README deve conter:

- Description;
- Instructions;
- Resources;
- formato completo do config.txt;
- algoritmo escolhido e justificativa;
- parte reutilizável do código;
- papéis da equipe;
- planejamento previsto e evolução;
- o que funcionou e o que pode melhorar;
- ferramentas utilizadas;
- uso de IA, indicando tarefas e partes do projeto em que foi usada.

NOTA (estado atual): o README.md atual só tem anotações de Git. Todo o conteúdo
acima ainda precisa ser escrito.


9. PASSOS NECESSÁRIOS PARA FINALIZAR
------------------------------------

Fase 1 - Validar o núcleo:

1. [x] revisar o contrato público de MazeGenerator e MazeSolver
	(MazeGenerator(width, height, entry, exit_coord, perfect, seed).generate()
	devolve a grade; MazeSolver(grid, entry, exit_coord).solve() devolve a lista
	de letras N/E/S/W);
2. [~] testar conectividade total (sem 42 está correto; com 42 quebra; sem teste);
3. [~] testar coerência das paredes vizinhas (existe teste, mas só sem 42; com
	o 42 há paredes incoerentes);
4. [x] testar bordas externas fechadas (teste existe e passa);
5. [~] testar PERFECT=True sem loops (a DFS gera árvore, mas não há teste e o 42
	quebra o resultado);
6. [~] implementar e testar as regras específicas de PERFECT=False;
7. [~] implementar e testar o padrão 42 (implementado, com bugs; sem testes;
	tamanho a definir, ver 1.2);
8. [ ] garantir que não exista área aberta 3x3 (regra do enunciado);
9. [ ] evitar que tamanhos pequenos quebrem o gerador (ex.: 2x2).

Fase 2 - Implementar a saída:

10. [x] implementar mazegen/encoder.py;
11. [x] converter a grade para hexadecimal;
12. [x] adicionar linha vazia, entrada, saída e caminho mínimo;
13. [x] testar o conteúdo e a formatação do arquivo.

Fase 3 - Integrar o programa:

14. [x] atualizar a_maze_ing.py para criar o gerador a partir de MazeConfig;
15. [x] gerar o labirinto;
16. [x] resolver o caminho;
17. [x] gravar OUTPUT_FILE;
18. [~] tratar ConfigError, FileNotFoundError e demais erros esperados (só
	FileNotFoundError está tratado);
19. [ ] alinhar o parser ao enunciado (ConfigError em todos os erros, SEED
	opcional, WIDTH/HEIGHT maiores que zero);
20. [ ] avisar quando não existir caminho entre entrada e saída.

Fase 4 - Finalizar a visualização:

21. [~] verificar o renderer escolhido (ASCII escolhido; 3 testes falham);
	decidir também o estilo: o atual desenha paredes com +---+; o exemplo do
	subject usa blocos preenchidos (mais legível para o 42 e para trocar as
	cores das paredes);
22. [~] mostrar paredes, entrada, saída e solução (mostra, mas o 42 não é
	destacado);
23. [ ] implementar o menu interativo do subject, repetindo até Quit:
	1. Re-generate a new maze (gera e exibe outro labirinto);
	2. Show / Hide the shortest path;
	3. Rotate the wall colours;
	4. Quit;
	com o prompt "Choice? (1-4):" e tratamento de entrada inválida;
24. [ ] testar a execução interativa;
25. [ ] (opcional) cores próprias para o padrão 42 (no exemplo, cinza).

Fase 5 - Empacotar e documentar:

26. [ ] preencher LICENSE.md;
27. [ ] preencher pyproject.toml (pacote mazegen-*);
28. [ ] gerar o pacote mazegen-*.whl ou .tar.gz na raiz e substituir o arquivo
	vazio atual;
29. [ ] criar os alvos do Makefile (install, run, debug, clean, lint,
	lint-strict);
30. [ ] escrever o README.md completo (seção 8) e a documentação de uso do
	gerador (seção 7).

Fase 6 - Validar a entrega:

31. [~] executar todos os testes (hoje 18 de 21 passam);
32. [ ] executar flake8 (hoje 139 avisos);
33. [x] executar mypy (sem erros hoje);
34. [~] executar python3 a_maze_ing.py config.txt (roda com o config.txt atual,
	mas ainda sem interação);
35. [ ] verificar o arquivo de saída com o analisador fornecido
	(maze_analyzer.py);
36. [ ] reconstruir o pacote mazegen a partir das fontes;
37. [ ] revisar README.md, LICENSE.md e o config.txt default.

Checklist obrigatório (estado atual):
- [x] Definir o contrato público de MazeGenerator e MazeSolver.
- [~] Garantir conectividade total, bordas externas fechadas e paredes vizinhas
      coerentes (bordas ok; conectividade e coerência quebram com o 42).
- [~] PERFECT=True sem loops (ok sem o 42; falta teste).
- [~] Regras específicas de PERFECT=False (implementação parcial; falta teste).
- [~] Implementar e testar o padrão 42 quando o tamanho permitir (implementado
      com bugs; sem testes).
- [ ] Garantir que não exista área aberta 3x3.
- [x] Implementar a serialização hexadecimal do labirinto no formato exigido.
- [x] Integrar a_maze_ing.py com a leitura da configuração e a gravação do
      arquivo de saída.
- [~] Tratar ConfigError, FileNotFoundError e erros esperados com mensagens
      claras (só FileNotFoundError).
- [~] Validar a renderização com entrada, saída e solução visíveis (visíveis
      em ASCII; 3 testes do renderer falham).
- [ ] Implementar as interações no formato do subject (menu 1-4: regenerar,
      mostrar/ocultar caminho, girar as cores das paredes, sair).
- [ ] Empacotar o mazegen (pyproject.toml, LICENSE.md e .whl válidos) e criar
      os alvos do Makefile.
- [ ] Executar todos os testes, flake8 e mypy sem falhas (mypy ok; testes 18 de
      21; flake8 com 139 avisos).
- [ ] Verificar o arquivo de saída com o analisador fornecido.
- [ ] Escrever o README.md completo e revisar LICENSE.md e config.txt default.


10. BÔNUS
---------

Possíveis bônus:

- modo não perfeito sem becos sem saída;
- múltiplos algoritmos de geração;
- animação durante a geração.

Bonuses
You may add various bonuses to your project. Here are possible examples:
• A default (non-perfect) maze with no dead-end at all: a perfectly “braided”
board, so a chased player is never trapped anywhere (the provided analysis script
confirms it with --max-dead-ends 0).
• Support multiple maze generation algorithms.
• Add animation during maze generation.

Checklist de bônus (opcional):
- [ ] Implementar um modo padrão sem dead-ends: labirinto "braided" sem becos sem saída.
- [ ] Validar que o tabuleiro continua jogável e sem armadilhas para o jogador perseguido.
- [ ] Usar o analisador fornecido com --max-dead-ends 0 para confirmar a regra bonus.
- [ ] Adicionar suporte a múltiplos algoritmos de geração (ex.: DFS, Prim, Eller, etc.).
- [ ] Permitir escolher o algoritmo via configuração ou CLI.
- [ ] Garantir que cada algoritmo respeite as regras do projeto.
- [ ] Implementar animação durante a geração do labirinto.
- [ ] Testar a animação para não quebrar a geração nem a renderização final.
- [ ] Documentar os bônus no README como melhorias extras do projeto.