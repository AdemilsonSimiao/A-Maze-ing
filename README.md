A-Maze-ing
criar clone
criar branch usando git checkout -b e-o-nome-da-branch // eu coloquei como switch
git checkout e-o-nome-da-branch muda para a branch que escolheu
exemplo, quando quero usar a bach swich eu uso git checkout switch, quando quero usar a main eu faço, git checkout main.
De preferencia para usar a branch criada.
verificar qual branch estou usando usa-se 
git status
antes de iniciar a codar primeiro atualiza, usando
git checkout main
git pull origin main
atualizar historico do servido usa
git fetch
Em caso de erro ou bloqueio.
Salve as alterações primeiro usando
git add .
git commit
git checkout main